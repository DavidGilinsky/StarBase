#!/bin/sh
# StarBase pre-removal: stop and disable the service before files go away.
set -e
case "$1" in
  remove|deconfigure)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl disable --now starbased.service >/dev/null 2>&1 || true
    fi
    ;;
esac
exit 0
